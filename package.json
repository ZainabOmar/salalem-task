{
  "name": "Otbo5ly-App-React",
  "version": "1.1.0",
  "description": "Transforming the front-end in Otbo5ly-App from AngularJS into React",
  "author": "Zainab Hammami",
  "private": true,
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "start": "live-server",
    "build": "babel . --out-dir compiled --presets=es2015,react --ignore=node_modules,compiled,server --source-maps inline",
    "presets": "es2015"
  },
  "dependencies": {
    "jquery": "^2.2.1",
    "lodash": "^4.6.1",
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0"
  }
}
